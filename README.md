> This project is based on a previous project, but rebuilt using JavaFX to make it more user-friendly and interactive.

# Building the Project
### 🔹🔸 To export the project for use on another machine, follow these steps:
- Generate an executable .jar file or Export the entire project as a ZIP file

### 🔹🔸 To run the project on another machine, the following items are required:
- The .jar file
- The db.properties file
- MySQL Connector
- JavaFX SDK
- Java (JDK) installed

🟡 *NOTE:*
If your JAR file is a FAT JAR, it may already include all dependencies, the project code, the MySQL Connector, and possibly JavaFX in a single file.
